<UserControl
	x:Class="FaceRetouching.App.Controls.PluginControl"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:local="clr-namespace:FaceRetouching.App.Controls"
	mc:Ignorable="d"
	x:Name="base"
	d:DesignHeight="450" d:DesignWidth="800">

	<Border Background="#eeeeee" Padding="5" CornerRadius="5" Margin="5">
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition />
				<RowDefinition />
				<RowDefinition />
			</Grid.RowDefinitions>

			<CheckBox x:Name="select" VerticalAlignment="Center" Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Margin="5" />
			<TextBlock Grid.Column="1" Grid.Row="0" FontWeight="Bold" Text="Имя плагина:" />
			<TextBlock Grid.Column="2" Grid.Row="0" TextAlignment="Right" TextWrapping="Wrap" Text="{Binding ElementName=base, Path=PluginName}" />
			<TextBlock Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1" FontWeight="Bold" Text="Описание плагина:" />
			<TextBlock Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="2" TextWrapping="Wrap" Text="{Binding ElementName=base, Path=PluginDescription}" />
		</Grid>
	</Border>
</UserControl>
